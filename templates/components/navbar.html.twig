<nav class="navbar navbar-expand-lg navbar navbar-light">
    <a class="navbar-brand" href="{{ path('app_dashboard') }}"><i class="far fa-credit-card" style="margin-right: 5px; "></i>Controllet</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
        <ul class="navbar-nav">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item">
                    <span class="nav-link"><i class="fas fa-user-alt" style="margin-right: 5px"></i>Hello {{ app.user.getFirstName }}</span>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Registration</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>